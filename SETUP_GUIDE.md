# راهنمای راه‌اندازی SMS Gateway برای TP-Link Archer MR400 v4.3

## ⚠️ قبل از هر چیز: یک اصلاح مهم دربارهٔ معماری

کد اولیه دو فرض اشتباه داشت:

1. **مسیرهای `/cgi-bin/luci/...`** مربوط به رابط **LuCI** هستند که فقط روی
   روترهای **OpenWrt** وجود دارد. Archer MR400 از فریمور اختصاصی TP-Link
   استفاده می‌کند و اصلاً چنین مسیرهایی ندارد؛ درخواست‌ها همیشه با خطا
   مواجه می‌شدند.
2. **این کار "TR-069" نیست.** TR-069 (پروتکل CWMP) برای مدیریت *از راه دور
   توسط سرور ACS اپراتور/ISP* است. TP-Link هم رسماً اعلام کرده این
   خانوادهٔ روترها اصلاً API یا دستور رسمی برای SMS ندارند.

راه‌حل واقعی: صحبت مستقیم با **API داخلی و غیررسمیِ پنل وب مودم** (همان
چیزی که مرورگر شما هنگام ورود به `tplinkmodem.net` صدا می‌زند).

### خبر خوب دربارهٔ سخت‌افزار شما

سخت‌افزار/فریمور شما:
```
Firmware Version: 1.5.0 0.9.1 v0001.0 Build 250912 Rel.56008n
Hardware Version: Archer MR400 v4.3 00000002
```

کتابخانهٔ متن‌باز و فعالانه نگه‌داری‌شدهٔ
[`tplinkrouterc6u`](https://pypi.org/project/tplinkrouterc6u/) در فهرست
رسمی روترهای پشتیبانی‌شده‌اش **دقیقاً "Archer MR400 V.4.3"** را دارد. یعنی
برخلاف تلاش قبلی (که برای سخت‌افزار/فریمور خیلی قدیمی‌تری نوشته شده بود
و احتمالاً کار نمی‌کرد)، این یکی مستقیماً برای مدل شما تست شده. این
کتابخانه روی PyPI است و با یک `pip install` نصب می‌شود؛ نیازی به clone
کردن دستی یک ریپوی جانبی نیست.

**ریسک:** این هم‌چنان یک روش reverse-engineered غیررسمی است. اگر TP-Link
با آپدیت فریمور رفتار پنل وب را عوض کند، ممکن است لازم شود نسخهٔ
`tplinkrouterc6u` را آپدیت کنید (نگهدارنده معمولاً سریع وصله می‌کند).

---

## ۱. پیش‌نیازها

- Python 3.10 یا بالاتر (این کتابخانه صراحتاً نیازمند 3.10+ است)
- Node.js 18 یا بالاتر (برای فرانت‌اند)
- دسترسی شبکه‌ای بک‌اند به مودم (مثلاً `192.168.1.1`)
- یوزرنیم/پسورد پنل وب مودم (همانی که برای ورود به `tplinkmodem.net`
  استفاده می‌کنید)

## ۲. نصب و پیکربندی بک‌اند

```bash
cd backend
python3 -m venv venv
source venv/bin/activate      # ویندوز: venv\Scripts\activate
pip install -r requirements.txt

cp .env.example .env
```

فایل `.env` را باز کنید و مقداردهی کنید (آدرس باید با `http://` یا
`https://` شروع شود):

```
ROUTER_HOST=http://192.168.1.1
ROUTER_USER=admin
ROUTER_PASSWORD=رمز_واقعی_مودم
ALLOWED_ORIGINS=http://localhost:5173
FLASK_DEBUG=false
PORT=5000
```

اجرای تستی:

```bash
python app.py
```

تست سریع با curl:

```bash
curl http://localhost:5000/api/status
curl http://localhost:5000/api/inbox
curl -X POST http://localhost:5000/api/send_sms \
  -H "Content-Type: application/json" \
  -d '{"phone":"09123456789","message":"تست"}'
```

اگر `/api/status` مقادیر واقعی سیگنال/نوع شبکه (مثلاً "4G LTE") برگرداند،
یعنی ورود به مودم موفق بوده.

## ۳. اجرای فرانت‌اند

```bash
cp .env.example .env      # در ریشهٔ پروژهٔ فرانت‌اند
# VITE_API_BASE_URL را روی آدرس بک‌اند تنظیم کنید (پیش‌فرض: http://localhost:5000)

npm install
npm run dev
```

سپس در مرورگر باز کنید؛ در تب «نمای کلی» باید «بک‌اند متصل» را ببینید.
اگر «بک‌اند در دسترس نیست» دیدید، آدرس/پورت و CORS (`ALLOWED_ORIGINS`) را
چک کنید.

## ۴. تست سناریوی کامل

1. تب «ارسال پیامک»: یک شماره و متن وارد کنید و «ارسال پیامک» را بزنید.
2. تب «صندوق دریافت»: با دکمهٔ «به‌روزرسانی» پیامک‌های دریافتی مودم را
   ببینید.

## ۵. اجرای پایدار (پروداکشن)

بک‌اند را با gunicorn اجرا کنید، نه با `python app.py`:

```bash
gunicorn -w 1 -b 0.0.0.0:5000 app:app
```

**نکته:** حتماً `-w 1` (فقط یک worker). قفل سراسری داخل کد فقط در یک
پروسه معنا دارد، و مودم هم فقط یک نشست ورود را در آن واحد می‌پذیرد؛ چند
worker باعث تداخل نشست‌ها می‌شود.

برای اجرای دائمی از systemd یا pm2 استفاده کنید، و برای HTTPS و دامنه از
یک reverse proxy مثل nginx جلوی آن قرار دهید. فرانت‌اند را هم با
`npm run build` بسازید و خروجی استاتیک را همان‌جا سرو کنید.

## ۶. نکات امنیتی

- هرگز `.env` را در گیت commit نکنید.
- `ALLOWED_ORIGINS` را فقط به دامنهٔ واقعی فرانت‌اند محدود کنید.
- `FLASK_DEBUG` در پروداکشن باید `false` باشد.
- اگر بک‌اند از بیرون شبکهٔ محلی در دسترس است، حتماً پشت HTTPS و با
  احراز هویت (مثلاً یک API key ساده در هدر) قرارش دهید؛ در حال حاضر هر
  کسی که به بک‌اند دسترسی داشته باشد می‌تواند پیامک بفرستد.
- توجه داشته باشید هر بار بک‌اند به مودم لاگین می‌کند؛ اگر هم‌زمان در
  پنل وب مودم لاگین باشید، نشست شما ممکن است قطع شود (و برعکس) — این
  محدودیت خودِ مودم است، نه باگ این پروژه.

## ۷. عیب‌یابی

- **خطای ورود (`اتصال یا ورود به مودم ناموفق بود`)**: یوزر/پسورد را با
  ورود دستی به `http://<IP-مودم>` تست کنید. مطمئن شوید `ROUTER_HOST`
  شامل `http://` است.
- **مودم به‌طور مداوم قطع نشست می‌دهد**: پنل وب مودم را در مرورگر باز
  نگه ندارید در حین تست بک‌اند (فقط یک نشست هم‌زمان مجاز است).
- **بعد از آپدیت فریمور کار نکرد**: نسخهٔ کتابخانه را آپدیت کنید:
  `pip install --upgrade tplinkrouterc6u` و به CHANGELOG پروژه در
  GitHub (`AlexandrErohin/TP-Link-Archer-C6U`) سر بزنید — نگهدارنده
  فعال است و مدل‌های جدید/فریمورهای جدید را مرتب اضافه می‌کند.
- **پیام «not implemented» یا `AttributeError` برای یک تابع خاص**: طبق
  مستندات کتابخانه، همهٔ توابع روی همهٔ مدل‌ها پیاده نشده‌اند؛ اگر مورد
  خاصی رخ داد، آن endpoint را در `app.py` داخل try/except بگیرید.

## اگر واقعاً به TR-069/CWMP نیاز دارید

اگر منظور شما مدیریت مودم از طریق **ACS اپراتور** است (نه یک اسکریپت
محلی)، این معماری کاملاً فرق دارد: نیاز به یک سرور ACS واقعی
(مثلاً [GenieACS](https://genieacs.com)) دارید که مودم به آن گزارش
می‌دهد، و SMS باید به‌صورت یک آبجکت/پارامتر اختصاصی (vendor extension)
در دیتامدل TR-069 مودم وجود داشته باشد — که در بیشتر مودم‌های خانگی
مصرفی (از جمله این مدل) اصلاً وجود ندارد.
